Startup mongodb server (must run in root):
1) In CLI, type mongod 
2) To access the mongodb terminal, type mongo in another terminal

To run backend web server:
1) Use terminal to access to the backend directory
2) run the command "node app.js"